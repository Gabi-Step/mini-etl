[project]
name = "mini-etl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "pandas>=3.0.1",
]

[dependency-groups]
test = [
    "pytest>=9.0.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]
